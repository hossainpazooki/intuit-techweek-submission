# Modeling stack for building the submission (the base requirements.txt only
# covers reading data + running the validator). The pipeline is sklearn-only by
# design (Python 3.14 lacks wheels for lightgbm/econml/etc.); see METHODOLOGY.md.
-r requirements.txt

# Pinned EXACTLY: HistGradientBoosting output shifts at the third decimal across
# sklearn releases (verified 1.8.0 vs 1.9.0), so the committed artifacts (scorecard,
# submission CSVs) only reproduce byte-identically under this version.
scikit-learn==1.8.0    # HistGradientBoosting hazard/PD model + isotonic calibration
matplotlib>=3.8        # EDA / calibration plots (optional)
